node_modules
dist
dist-ssr
*.local
.env
.env.local
coverage
.DS_Store

# Local Claude Code "impeccable" skill install + its cache/config -- not project source
.claude/skills/impeccable/
.impeccable/

# Session scratch space -- never source
scratchpad/
