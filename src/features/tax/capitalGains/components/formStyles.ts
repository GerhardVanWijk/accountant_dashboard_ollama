/**
 * Shared Tailwind class strings for the Capital Gains page's inline
 * selling-costs input — semantic design tokens only, no hardcoded colors
 * (docs/DESIGN_SYSTEM.md). Mirrors
 * src/features/assets/components/formStyles.ts /
 * src/features/inventory/components/formStyles.ts — each feature keeps
 * its own copy rather than sharing one across feature folders.
 */
export const fieldInput =
  'w-full rounded-md border border-border bg-background px-sm py-xs text-right text-sm tabular-nums text-text-primary outline-none focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-1 focus-visible:outline-primary';
